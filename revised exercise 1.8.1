iteration = int(input("how many numbers are you gonna add? "))
maximum_V = -1000

for i in range(iteration):
    input_numbers = int(input("Enter a number "))
    if input_numbers > maximum_V:
        maximum_V = input_numbers
print(maximum_V)
